[kernel] Parsing share/libc/__fc_builtin_for_normalization.i (no preprocessing)
[kernel] Parsing tests/rte/assign4.c (with preprocessing)
[rte] annotating function main
/* Generated by Frama-C */
/*@ assigns \result;
    assigns \result \from min, max; */
extern int choose1(int min, int max);

/*@ assigns \result, \result;
    assigns \result \from min, max, min, max;
    assigns \result \from min, max, min, max;
 */
extern int choose2(int min, int max);

int main(void)
{
  int __retres;
  int c1;
  int c2;
  /*@ behavior pre_choose1:
        assigns c1;
        assigns c1 \from \nothing; */
  c1 = choose1(5,10);
  /*@ behavior pre_choose2:
        assigns c2, c2;
        assigns c2 \from c1, c1;
        assigns c2 \from c1, c1;
  */
  c2 = choose2(0,c1);
  __retres = c1 + c2;
  return __retres;
}


