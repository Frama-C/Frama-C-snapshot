
int A,B,C,D,E,F,G,H,I,J,K;

void main (int i)
{
  A = (4 * i) % 4;
  B = (4 * i + 1) % 4;
  //@ assert ((i>=-100) && (i<=100)) ;
  E = (3*i + 1) % 12; 
  //@ assert ((i>=0) && (i<=100)) ;
    
  C = (4 * i + 1) % 4;
  D = (3*i + 1) % 12; 
  F = (24*i + 5) % 12;    
  G = (24*i + 5) % 13;    
  H = i % 1000;
  I = (2 * i+1101) % 1000;
  J = (5 * i - 201) % 1000;
  K = (5 * i - 201) % 10;
}
