[kernel] Parsing tests/spec/separated.c (with preprocessing)
/* Generated by Frama-C */
/*@ requires \base_addr(q) ≢ p;
    requires \separated(p, q);
    requires \valid(p + (0 .. n - 1));
    requires \valid(q + (0 .. n - 1));
    assigns *(p + (0 .. n - 1));
 */
void put(char *p, char *q, int n)
{
  int i = 0;
  while (i < n) {
    {
      char *tmp;
      char *tmp_0;
      /*@ assert \separated(p, q); */ ;
      tmp = p;
      p ++;
      tmp_0 = q;
      q ++;
      *tmp = *tmp_0;
    }
    i ++;
  }
  return;
}


