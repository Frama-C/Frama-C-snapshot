[kernel] Parsing share/libc/__fc_builtin_for_normalization.i (no preprocessing)
[kernel] Parsing tests/spec/homax.c (with preprocessing)
/* Generated by Frama-C */
int max_seq(int *p, int n);

/*@ requires n > 0 ∧ \valid(p+(0 .. n-1));
    ensures \result ≡ \max(0, \old(n)-1, \lambda ℤ i; *(\old(p)+i));
 */
int max_seq(int *p, int n)
{
  int res;
  res = *p;
  {
    int i;
    i = 0;
    while (i < n) {
      if (res < *p) res = *p;
      p ++;
      i ++;
    }
  }
  return res;
}


