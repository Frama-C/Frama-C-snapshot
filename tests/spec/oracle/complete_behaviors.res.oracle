[kernel] Parsing share/libc/__fc_builtin_for_normalization.i (no preprocessing)
[kernel] Parsing tests/spec/complete_behaviors.c (with preprocessing)
/* Generated by Frama-C */
enum __anonenum_kind_1 {
    Max = 0,
    Min = 1
};
typedef enum __anonenum_kind_1 kind;
/*@ requires k ≡ Max ∨ k ≡ Min;
    ensures \result ≡ \old(x) ∨ \result ≡ \old(y);
    assigns \nothing;
    
    behavior is_max:
      assumes k ≡ Max;
      ensures \result ≥ \old(x) ∧ \result ≥ \old(y);
    
    behavior is_min:
      assumes k ≡ Min;
      ensures \result ≤ \old(x) ∧ \result ≤ \old(y);
    
    complete behaviors is_max, is_min;
    disjoint behaviors is_max, is_min;
 */
int extremum(kind k, int x, int y)
{
  int tmp_0;
  int tmp;
  if (k == (unsigned int)Max) tmp = x > y; else tmp = x < y;
  if (tmp) tmp_0 = x; else tmp_0 = y;
  return tmp_0;
}


