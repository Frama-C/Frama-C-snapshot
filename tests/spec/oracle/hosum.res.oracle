[kernel] Parsing tests/spec/hosum.c (with preprocessing)
/* Generated by Frama-C */
int sqsum(int *p, int n);

/*@ requires n ≥ 0;
    requires \valid(p + (0 .. n - 1));
    ensures
      \result ≡
      \sum(0, \old(n) - 1, \lambda ℤ i; *(\old(p) + i) * *(\old(p) + i));
    assigns \nothing;
 */
int sqsum(int *p, int n)
{
  int tmp;
  int S = 0;
  {
    int i = 0;
    while (i < n) {
      /*@ assert *(p + i) * *(p + i) ≤ 1 << (30 - 1); */ ;
      tmp = *(p + i) * *(p + i);
      /*@ assert tmp ≥ 0; */ ;
      /*@ assert S + tmp ≤ 1 << (30 - 1); */ ;
      S += tmp;
      i ++;
    }
  }
  return S;
}


